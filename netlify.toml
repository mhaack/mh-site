[build]
  command = "npm run build"
  publish = "dist"
  # functions = "./src/functions"

[dev]
  command = "npm run start"
  framework = "#custom"
  publish = "dist"
  port = 3000
  targetPort = 8181
  NODE_ENV = "development"
