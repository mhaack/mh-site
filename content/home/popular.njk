---
theme:
  background: bg-rob-roy-200
  text: text-gray-900
order: 3
sitemap:
  ignore: true
---

{%- set postslist = collections.posts | popularPosts(analytics) | head(5) -%}

<h2 class="mb-8">The most popular projects & posts - <a href="/popular/">view all</a></h2>
<ol class="list-decimal list-inside mx-auto lg:max-w-none text-2xl bg-white shadow-lg p-4">
  {% for post in postslist %}
  <li class="my-2">
    <a href="{{ post.url | url }}">{{ post.data.title }}</a>
  </li>
  {% endfor %}
</ol>
